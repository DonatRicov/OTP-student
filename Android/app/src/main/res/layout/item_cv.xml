<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginBottom="8dp"

    android:gravity="center_vertical">

    <!-- This ID fixes 'imgPdfIcon' -->
    <ImageView
        android:id="@+id/imgPdfIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@android:drawable/ic_menu_view"
        android:layout_marginEnd="12dp"
        android:contentDescription="PDF Icon" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- This ID fixes 'tvFileName' -->
        <TextView
            android:id="@+id/tvFileName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="zivotopis.pdf"
            android:textStyle="bold"
            android:textColor="#000000"/>

        <!-- This ID fixes 'tvUploaderName' -->
        <TextView
            android:id="@+id/tvUploaderName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Datum i vrijeme"
            android:textSize="12sp"
            android:textColor="#666666"/>
    </LinearLayout>

    <!-- This ID fixes 'btnDelete' -->
    <ImageView
        android:id="@+id/btnDelete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/ic_menu_delete"
        android:padding="8dp"
        android:contentDescription="Izbriši" />

</LinearLayout>
